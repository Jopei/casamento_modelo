# Deixe vazio para que a API seja procurada no mesmo host que serviu o site
# (porta 8090). Assim o site funciona tanto em localhost quanto pelo IP da
# rede local, no celular. Preencha apenas se a API ficar em outro endereco.
VITE_API_URL=
